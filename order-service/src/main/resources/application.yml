server:
  port: 9981
spring:
  application:
    name: Microservice
  datasource:
    username: root
    password: ola24oluwa
    url: jdbc:mysql://localhost:3306/6-hour
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
